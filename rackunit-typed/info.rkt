#lang info

(define collection "typed")

(define test-responsibles '((all jay)))

(define deps
  '("racket-index"
    "rackunit-gui"
    "rackunit-lib"
    "typed-racket-lib"
    "base"
    "testing-util-lib"))

(define version "1.0")
